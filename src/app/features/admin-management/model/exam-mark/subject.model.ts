export interface Subject {
    subjectCode: string; // The unique code for the subject
    subjectName: string; // The name of the subject
    theoreticalScore?: number; // Theoretical score, optional
    practicalScore?: number; // Practical score, optional
  }
  